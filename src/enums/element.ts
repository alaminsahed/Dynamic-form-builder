export enum SELECTED_ELEMENT {
  MAIN = 'MAIN',
  STYLE = 'STYLE',
  RULES = 'RULES',
}

export enum ELEMENT_TYPE {
  TEXT = 'text',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  SELECT = 'select',
  TEXTAREA = 'textarea',
}
